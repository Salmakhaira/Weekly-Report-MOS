{
  "name": "sales-report-monitoring",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "db:push": "mkdir -p data && drizzle-kit push --force",
    "db:seed": "tsx src/db/seed.ts",
    "setup": "mkdir -p data && drizzle-kit push --force && tsx src/db/seed.ts",
    "db:reset": "rm -rf data && mkdir -p data && drizzle-kit push --force && tsx src/db/seed.ts"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "drizzle-orm": "^0.45.2",
    "exceljs": "^4.4.0",
    "next": "15.5.24",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.10.7",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "autoprefixer": "^10.4.20",
    "drizzle-kit": "^0.31.10",
    "postcss": "^8.5.1",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}